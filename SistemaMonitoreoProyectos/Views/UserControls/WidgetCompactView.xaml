<UserControl x:Class="SistemaMonitoreoProyectos.Views.UserControls.WidgetCompactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Border Background="{StaticResource BrocheFondoTarjeta}"
            BorderBrush="{StaticResource BrocheBordeTarjeta}"
            BorderThickness="1"
            CornerRadius="16"
            Padding="8,6,16,6"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            
            <!-- Avatar / Icono -->
            <Border Width="28" Height="28" CornerRadius="14" Background="#2E2E35" Margin="0,0,10,0">
                <TextBlock Text="👤" Foreground="{StaticResource BrocheTextoPrincipal}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12"/>
            </Border>

            <!-- Título de la tarea -->
            <TextBlock x:Name="TextoNombreActividadMinimizada" Text="APP WEB 1" Foreground="{StaticResource BrocheTextoPrincipal}" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>

            <!-- Separador Vertical -->
            <Rectangle Width="1" Height="16" Fill="{StaticResource BrocheBordeTarjeta}" Margin="12,0"/>

            <!-- Cronómetro compacto -->
            <TextBlock x:Name="TextoRelojMinimizado" Text="42:15" Foreground="{StaticResource BrocheTextoPrincipal}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>